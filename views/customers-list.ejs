<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- bootstrap css -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!-- main css -->
    <link rel="stylesheet" href="/styles/main.css">
    <title>Point of Sale</title>
    <style>
        thead, tr:nth-child(even) {
            background-color: rgba(0, 0, 0, 0.137);
        }
    </style>
</head>
<body style="width: 500px; margin:auto;">

    <h2>Welcome to the Customers List</h2>
    <a class="btn btn-lg btn-info" href="/">Home</a>
    <!-- This is ugly AF, but gets the job done for debugging. We'll def refactor and throw this out later -->
    <table id="cust-table">
        <thead>
            <tr>
                <th>key</th>
                <th>val</th>
            </tr>
        </thead>
        <tbody>
            <% for (var x in result) { %>
                <% if (x === "address" || x === "preferences") { %>
                    <% for (var a in result[x]) { %>
                        <tr>
                            <td style="padding-left: 10px;"><%= a %></td>
                            <td style="padding-left: 10px;"><%= result[x][a] %></td>
                        </tr>
                    <% } %>
                <% } else { %>
                    <tr>
                        <td><%= x %></td>
                        <td><%= result[x] %></td>
                    </tr>
                <% } %>
            <% } %>
        </tbody>
    </pre>
    
    <!-- bootstrap js bundle import -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <!-- sign in scrpts -->
    <script src="/scripts/signin.js"></script>
</body>
</html>